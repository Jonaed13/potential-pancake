## 2024-05-22 - Keybinding Conflicts in TUI
**Learning:** Single-character hotkeys (like 't') are easily overloaded in TUIs, causing unreachable actions (e.g., 'Trades' vs 'Theme').
**Action:** Use Shift+Key (e.g., 'T') for secondary or less frequent actions to avoid conflicts with primary navigation keys.
